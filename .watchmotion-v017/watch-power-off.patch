--- watchmotion_v016_base/source/phone/build.gradle.kts	2026-08-27 17:34:06.000000000 +0000
+++ watchmotion_v017_work/source/phone/build.gradle.kts	2026-08-28 00:08:27.002035785 +0000
@@ -11,8 +11,8 @@
         applicationId = "com.irisproxima.watchmotion"
         minSdk = 26
         targetSdk = 36
-        versionCode = 6
-        versionName = "0.1.6"
+        versionCode = 7
+        versionName = "0.1.7"
     }
 
     signingConfigs {
--- watchmotion_v016_base/source/wear/build.gradle.kts	2026-08-27 17:34:06.000000000 +0000
+++ watchmotion_v017_work/source/wear/build.gradle.kts	2026-08-28 00:08:27.002198595 +0000
@@ -11,8 +11,8 @@
         applicationId = "com.irisproxima.watchmotion"
         minSdk = 30
         targetSdk = 36
-        versionCode = 6
-        versionName = "0.1.6"
+        versionCode = 7
+        versionName = "0.1.7"
     }
 
     signingConfigs {
--- watchmotion_v016_base/source/phone/src/main/java/com/irisproxima/watchmotion/Protocol.kt	2026-08-27 04:39:16.000000000 +0000
+++ watchmotion_v017_work/source/phone/src/main/java/com/irisproxima/watchmotion/Protocol.kt	2026-08-28 00:08:27.000876157 +0000
@@ -23,6 +23,7 @@
     PREVIOUS("PREVIOUS"),
     NEXT("NEXT"),
     TV_WAKE("TV_WAKE"),
+    TV_POWER_OFF("TV_POWER_OFF"),
     PING("PING");
 
     companion object {
--- watchmotion_v016_base/source/phone/src/main/java/com/irisproxima/watchmotion/CommandRouter.kt	2026-08-27 15:07:26.000000000 +0000
+++ watchmotion_v017_work/source/phone/src/main/java/com/irisproxima/watchmotion/CommandRouter.kt	2026-08-28 00:08:27.001064744 +0000
@@ -34,6 +34,42 @@
             }
         }
 
+        if (command == RemoteCommand.TV_POWER_OFF) {
+            prefs.edit()
+                .putString("last_command", command.wire)
+                .putLong("last_command_time", System.currentTimeMillis())
+                .apply()
+            broadcastCommand(appContext, command)
+
+            val device = DeviceStore.tvShotDevice(appContext)
+            if (device == null) {
+                val detail = "No TV selected for watch power control"
+                prefs.edit()
+                    .putString("last_tv_result", detail)
+                    .putLong("last_tv_result_time", System.currentTimeMillis())
+                    .apply()
+                appContext.sendBroadcast(
+                    Intent(Protocol.ACTION_TV_UI)
+                        .setPackage(appContext.packageName)
+                        .putExtra(Protocol.EXTRA_DETAIL, detail)
+                )
+                return RouteResult(mappedDetail = detail)
+            }
+
+            LgWebOsController.powerOff(appContext, device) { result ->
+                prefs.edit()
+                    .putString("last_tv_result", result.detail)
+                    .putLong("last_tv_result_time", System.currentTimeMillis())
+                    .apply()
+                appContext.sendBroadcast(
+                    Intent(Protocol.ACTION_TV_UI)
+                        .setPackage(appContext.packageName)
+                        .putExtra(Protocol.EXTRA_DETAIL, result.detail)
+                )
+            }
+            return RouteResult(mappedDeviceQueued = true, mappedDetail = "${device.displayName}: power off queued")
+        }
+
         if (command == RemoteCommand.TV_WAKE) {
             prefs.edit()
                 .putString("last_command", command.wire)
--- watchmotion_v016_base/source/phone/src/main/java/com/irisproxima/watchmotion/MainActivity.kt	2026-08-27 17:36:07.000000000 +0000
+++ watchmotion_v017_work/source/phone/src/main/java/com/irisproxima/watchmotion/MainActivity.kt	2026-08-28 00:08:27.002597286 +0000
@@ -119,7 +119,7 @@
             setTypeface(typeface, Typeface.BOLD)
         })
         root.addView(TextView(this).apply {
-            text = "v0.1.6 • Watch gestures → Android / Windows / network devices"
+            text = "v0.1.7 • Watch gestures → Android / Windows / network devices"
             textSize = 14f
             setPadding(0, dp(2), 0, dp(18))
         })
@@ -518,6 +518,7 @@
             Toast.makeText(this, "This device needs an IP / host first", Toast.LENGTH_LONG).show()
             return
         }
+        DeviceStore.setTvShotDevice(this, device.id)
         tvStatusText.text = "Connecting to ${device.displayName}…"
         LgWebOsController.pair(this, device) { result ->
             runOnUiThread {
--- watchmotion_v016_base/source/phone/src/main/java/com/irisproxima/watchmotion/MediaControllerBridge.kt	2026-08-27 04:39:16.000000000 +0000
+++ watchmotion_v017_work/source/phone/src/main/java/com/irisproxima/watchmotion/MediaControllerBridge.kt	2026-08-28 00:08:27.001263196 +0000
@@ -16,6 +16,7 @@
     fun execute(command: RemoteCommand): Result {
         if (command == RemoteCommand.PING) return Result(true, "Ping ignored by Android media")
         if (command == RemoteCommand.TV_WAKE) return Result(false, "TV_WAKE is handled by the LG TV controller")
+        if (command == RemoteCommand.TV_POWER_OFF) return Result(false, "TV_POWER_OFF is handled by LG webOS control")
 
         val controller = chooseController()
         if (controller != null) {
@@ -74,6 +75,7 @@
             }
 
             RemoteCommand.TV_WAKE -> Result(false, "TV_WAKE is handled by the LG TV controller")
+            RemoteCommand.TV_POWER_OFF -> Result(false, "TV_POWER_OFF is handled by LG webOS control")
             RemoteCommand.PING -> Result(true, "Ping")
         }
     }
@@ -112,6 +114,7 @@
             RemoteCommand.SEEK_FORWARD -> KeyEvent.KEYCODE_MEDIA_FAST_FORWARD
             RemoteCommand.SEEK_BACK -> KeyEvent.KEYCODE_MEDIA_REWIND
             RemoteCommand.TV_WAKE -> return Result(false, "TV_WAKE is handled by the LG TV controller")
+            RemoteCommand.TV_POWER_OFF -> return Result(false, "TV_POWER_OFF is handled by LG webOS control")
             RemoteCommand.PING -> return Result(true, "Ping")
         }
 
--- watchmotion_v016_base/source/wear/src/main/java/com/irisproxima/watchmotion/Protocol.kt	2026-08-27 04:39:16.000000000 +0000
+++ watchmotion_v017_work/source/wear/src/main/java/com/irisproxima/watchmotion/Protocol.kt	2026-08-28 00:08:27.001017653 +0000
@@ -13,5 +13,6 @@
     PLAY_PAUSE("PLAY_PAUSE"),
     PREVIOUS("PREVIOUS"),
     NEXT("NEXT"),
-    TV_WAKE("TV_WAKE")
+    TV_WAKE("TV_WAKE"),
+    TV_POWER_OFF("TV_POWER_OFF")
 }
--- watchmotion_v016_base/source/wear/src/main/java/com/irisproxima/watchmotion/MainActivity.kt	2026-08-27 17:34:06.000000000 +0000
+++ watchmotion_v017_work/source/wear/src/main/java/com/irisproxima/watchmotion/MainActivity.kt	2026-08-28 00:08:27.001892376 +0000
@@ -101,7 +101,7 @@
         }, fullWidth())
 
         root.addView(TextView(this).apply {
-            text = "v0.1.6 • media + mapped devices"
+            text = "v0.1.7 • media + mapped devices"
             textSize = 11f
             setTextColor(Color.GRAY)
             gravity = Gravity.CENTER
@@ -163,18 +163,13 @@
             "Next" to RemoteCommand.NEXT
         ), fullWidth())
 
-        root.addView(Button(this).apply {
-            text = "Wake LG TV"
-            setOnClickListener {
-                WatchMessenger.send(this@MainActivity, RemoteCommand.TV_WAKE) { ok, detail ->
-                    engineStatus.text = detail
-                    if (!ok) Toast.makeText(this@MainActivity, detail, Toast.LENGTH_SHORT).show()
-                }
-            }
-        }, fullWidth())
+        root.addView(commandRow(
+            "Wake TV" to RemoteCommand.TV_WAKE,
+            "Power Off TV" to RemoteCommand.TV_POWER_OFF
+        ), fullWidth())
 
         root.addView(TextView(this).apply {
-            text = "Manual buttons test watch → phone independently of gesture tuning. Configure/map the TV in the phone app first."
+            text = "Manual TV buttons test watch → phone independently of gesture tuning. Wake uses WOL; Power Off uses the saved LG webOS pairing."
             textSize = 11f
             setTextColor(Color.GRAY)
             gravity = Gravity.CENTER
--- watchmotion_v016_base/source/build-v0.1.bat	2026-08-27 17:36:07.000000000 +0000
+++ watchmotion_v017_work/source/build-v0.1.bat	2026-08-28 00:08:27.002749520 +0000
@@ -3,7 +3,7 @@
 cd /d "%~dp0"
 
 echo ============================================================
-echo   Watch Motion Controller v0.1.6 - Device Quick Add / Mapper
+echo   Watch Motion Controller v0.1.7 - Device Quick Add / Mapper
 echo ============================================================
 echo.
 
@@ -67,16 +67,16 @@
 )
 
 if not exist dist mkdir dist
-copy /Y "phone\build\outputs\apk\debug\phone-debug.apk" "dist\WatchMotionController-Phone-v0.1.6.apk" >nul
-copy /Y "wear\build\outputs\apk\debug\wear-debug.apk" "dist\WatchMotionController-Wear-v0.1.6.apk" >nul
+copy /Y "phone\build\outputs\apk\debug\phone-debug.apk" "dist\WatchMotionController-Phone-v0.1.7.apk" >nul
+copy /Y "wear\build\outputs\apk\debug\wear-debug.apk" "dist\WatchMotionController-Wear-v0.1.7.apk" >nul
 rem Legacy aliases preserve the original v0.1 workflow/file names.
 copy /Y "phone\build\outputs\apk\debug\phone-debug.apk" "dist\WatchMotionController-Phone-v0.1.apk" >nul
 copy /Y "wear\build\outputs\apk\debug\wear-debug.apk" "dist\WatchMotionController-Wear-v0.1.apk" >nul
 
 echo.
 echo [OK] Built:
-echo   dist\WatchMotionController-Phone-v0.1.6.apk
-echo   dist\WatchMotionController-Wear-v0.1.6.apk
+echo   dist\WatchMotionController-Phone-v0.1.7.apk
+echo   dist\WatchMotionController-Wear-v0.1.7.apk
 echo   ^(legacy aliases also refreshed: *-v0.1.apk^)
 echo.
 pause
--- watchmotion_v016_base/source/.github/workflows/build.yml	2026-08-27 17:36:07.000000000 +0000
+++ watchmotion_v017_work/source/.github/workflows/build.yml	2026-08-28 00:08:27.002794657 +0000
@@ -21,11 +21,11 @@
       - name: Collect APKs
         run: |
           mkdir -p dist
-          cp phone/build/outputs/apk/debug/phone-debug.apk dist/WatchMotionController-Phone-v0.1.6.apk
-          cp wear/build/outputs/apk/debug/wear-debug.apk dist/WatchMotionController-Wear-v0.1.6.apk
+          cp phone/build/outputs/apk/debug/phone-debug.apk dist/WatchMotionController-Phone-v0.1.7.apk
+          cp wear/build/outputs/apk/debug/wear-debug.apk dist/WatchMotionController-Wear-v0.1.7.apk
       - uses: actions/upload-artifact@v4
         with:
-          name: WatchMotionController-Android-v0.1.6
+          name: WatchMotionController-Android-v0.1.7
           path: dist/*.apk
 
   windows:
